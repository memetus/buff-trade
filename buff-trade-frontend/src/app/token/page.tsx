import React from "react";
import styles from "@/app/token/page.module.scss";
import classNames from "classnames/bind";

const cx = classNames.bind(styles);

const TokenPage = () => {
  return <div></div>;
};

export default TokenPage;
