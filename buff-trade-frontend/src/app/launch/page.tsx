import React from "react";
import styles from "@/app/launch/page.module.scss";
import classNames from "classnames/bind";

const cx = classNames.bind(styles);

const LaunchPage = () => {
  return <div></div>;
};

export default LaunchPage;
