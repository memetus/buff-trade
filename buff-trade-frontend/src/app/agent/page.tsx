import React from "react";
import styles from "@/app/agent/page.module.scss";
import classNames from "classnames/bind";

const cx = classNames.bind(styles);

const AgentPage = () => {
  return <div></div>;
};

export default AgentPage;
